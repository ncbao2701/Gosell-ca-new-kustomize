apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: callcenter-services
  name: callcenter-services
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  replicas: 1
  selector:
    matchLabels:
      app: callcenter-services
  strategy: {}
  template:
    metadata:
      labels:
        app: callcenter-services
    spec:
      containers:
      - image: callcenter-services
        name: callcenter-services
        ports:
          - name: service
            containerPort: 8083
            protocol: TCP
          - name: hazale
            containerPort: 5701
            protocol: UDP
        resources:
          limits:
            # cpu: '0.5'
            memory: 1Gi
          requests:
            # cpu: '0.2'
            memory: 200Mi

        livenessProbe:
          httpGet:
            path: /management/health
            port: 8083
            scheme: HTTP
          initialDelaySeconds: 600
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /management/health
            port: 8083
            scheme: HTTP
          initialDelaySeconds: 120
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        envFrom:
          - secretRef:
              name: callcenter-services
      imagePullSecrets:
        - name: fpt-cloud-registry
status: {}
---
