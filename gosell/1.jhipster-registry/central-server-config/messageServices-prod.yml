#common configuration shared between all applications
beecow:
    debugMode: true
    messageServices:
        firebase:
            private-key-file-path: http://firebase-project.s3.amazonaws.com/firebase-admin-dev.json
            url: https://beecow-dev.firebaseio.com
            rootPath:
            userNode: Users
            roomNode: Rooms
            timeout: 30
            timeoutUnit: SECONDS
            privateKeyGosell:
                - name: beecow-dev
                  filePath: http://firebase-project.s3.amazonaws.com/firebase-admin-dev.json
spring:
    data:
        jest:
            uri: http://es5.mediastep.com:9202