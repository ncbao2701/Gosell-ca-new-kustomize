apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cashbook-services
  name: cashbook-services
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  replicas: 1
  selector:
    matchLabels:
      app: cashbook-services
  strategy: {}
  template:
    metadata:
      labels:
        app: cashbook-services
    spec:
      containers:
      - image: cashbook-services
        name: cashbook-services
        ports:
        - name: service
          containerPort: 8284
          protocol: TCP
        resources:
          limits:
            cpu: '0.5'
            memory: 1Gi
          requests:
            cpu: '0.2'
            memory: 200Mi

        livenessProbe:
          tcpSocket:
            port: 8284
          initialDelaySeconds: 600
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8284
          initialDelaySeconds: 120
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        envFrom:
          - secretRef:
              name: cashbook-services
      imagePullSecrets:
        - name: fpt-cloud-registry
status: {}
---
