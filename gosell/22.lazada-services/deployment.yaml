apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: lazada-services
  name: lazada-services
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  replicas: 1
  selector:
    matchLabels:
      app: lazada-services
  strategy: {}
  template:
    metadata:
      labels:
        app: lazada-services
    spec:
      containers:
      - image: lazada-services
        name: lazada-services
        ports:
        - name: service
          containerPort: 8081
          protocol: TCP
        - name: hazale
          containerPort: 5701
          protocol: UDP
        resources:
          limits:
            cpu: '0.5'
            memory: 1Gi
          requests:
            cpu: '0.2'
            memory: 200Mi

        livenessProbe:
          tcpSocket:
            port: 8082
          initialDelaySeconds: 600
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8082
          initialDelaySeconds: 120
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        envFrom:
          - secretRef:
              name: lazada-services
      imagePullSecrets:
        - name: fpt-cloud-registry
status: {}
---
