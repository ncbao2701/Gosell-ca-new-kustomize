apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: message-services
  name: message-services
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  replicas: 1
  selector:
    matchLabels:
      app: message-services
  strategy: {}
  template:
    metadata:
      labels:
        app: message-services
    spec:
      containers:
      - image: message-services
        name: message-services
        ports:
        - name: service
          containerPort: 8400
          protocol: TCP
        resources:
          limits:
            cpu: '0.5'
            memory: 1Gi
          requests:
            cpu: '0.2'
            memory: 200Mi
        livenessProbe:
          tcpSocket:
            port: 8400
          initialDelaySeconds: 600
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8400
          initialDelaySeconds: 120
          timeoutSeconds: 1
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        envFrom:
          - secretRef:
              name: message-services
      imagePullSecrets:
        - name: fpt-cloud-registry
status: {}
---
